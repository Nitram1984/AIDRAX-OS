# Integration Gate

AO-002 is package-local. Main-runtime integration requires owner approval for:

1. persistent identity schema plus migration and rollback;
2. policy source and administrator workflow;
3. HERMES event schema and ARGUS retention policy;
4. local-only assurance and real desktop acceptance tests.

Until then there is no service, listener, account store, or credential path.
