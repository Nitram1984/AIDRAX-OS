# Retention and Integration Gate

AO-003 does not choose a production path, retention period, encryption policy, or backup mechanism. Those need explicit owner approval before integration.
The main runtime stays untouched until an identity-binding, storage, access, retention, export, and recovery contract is approved.
