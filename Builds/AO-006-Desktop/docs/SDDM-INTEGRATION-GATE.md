# SDDM Integration Gate

AO-006 is owner-approved as an isolated desktop-shell release, but does not install a theme or alter the active display manager. A later SDDM integration needs a separately owner-approved theme package, a static poster fallback for any decorative video, verified authentication behavior, and a tested restore procedure for the prior theme. Desktop controls remain proposals until the AIDRAX Owner Gate authorizes an executor.
