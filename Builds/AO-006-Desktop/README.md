# AIDRAX OS — AO-006 Desktop Shell

AO-006 provides a local desktop-shell domain for lock/ready presentation and owner-gated system-control proposals. It does not install or configure SDDM, open a GUI, authenticate a user, or invoke restart/power commands.
