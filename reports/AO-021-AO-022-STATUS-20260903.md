# AO-021 / AO-022 Technical Status — 2026-09-03

## Owner decision

The AIDRAX Owner approved continued controlled work until the stages can show
their own evidence-backed GREEN status. The decision does not waive dependency,
integrity, network-isolation, host-safety, or target-acceptance gates.

## AO-021 — GREEN on isolated work copy

Source rootfs:
`Builds/AO-020-Isolated-Rootfs-Assembly/build-output/rootfs-ao021-version-safe`

Result rootfs (new isolated copy):
`Builds/AO-020-Isolated-Rootfs-Assembly/build-output/rootfs-ao021-green-attempt-20260903`

Evidence:

- Rootless user-namespace copy; source rootfs was not modified.
- `dpkg --configure -a` ran with a new network namespace and the existing
  `policy-rc.d` service-start denial.
- Six pending packages configured immediately.
- `dracut-install` was blocked only by missing `libkmod2`.
- The local AO-019 `libkmod2_31+20240202-2ubuntu7_amd64.deb` matched the
  lock SHA-256 `43858c80a67c3a8a5a10d8e6769e7d92614d9eb19cb14e159b4311a16b9cf54b`.
- After its isolated installation, `dracut-install` configured successfully;
  `dpkg --audit` was empty and all package records were `install ok installed`.

This proves rootfs package configuration only. It does not prove bootability.

## AO-022 — BLOCKED, genuine missing dependency

The configured rootfs contains kernel `6.8.0-31-generic`, `depmod`, and
`lsinitramfs`, but lacks `/usr/sbin/update-initramfs`. The current AO-019 lock
has `initramfs-tools-bin` and `initramfs-tools-core`, but not the required
`initramfs-tools` package. No local `initramfs-tools_*.deb` is available.

Required next input: add `initramfs-tools` to a compatible, signed and
hash-verified closure lock; materialize and hash-verify its exact Debian
package; then install it only into a new isolated AO-022 work copy before
generating and inspecting the initramfs. No download, ISO, bootloader,
firmware, storage, or host-desktop action was performed.
